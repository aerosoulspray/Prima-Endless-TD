using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StarterPackGanern : MonoBehaviour
{
    public GameObject paneler;
    // Start is called before the first frame update
    void Start()
    {
        paneler.SetActive(false);
    }

}
